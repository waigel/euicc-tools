{
  "$comment": "A TextMate grammar for ASN.1 value notation. Shared with asn1-docs, which uses it to colour schema listings. Highlighting says whether text looks like ASN.1. Whether it is a correct profile is what euicc check says.",
  "name": "asn1-vn",
  "displayName": "ASN.1 value notation",
  "aliases": [
    "asn1vn"
  ],
  "scopeName": "source.asn1-vn",
  "patterns": [
    {
      "include": "#comment-line"
    },
    {
      "include": "#comment-block"
    },
    {
      "include": "#hexstring"
    },
    {
      "include": "#binstring"
    },
    {
      "include": "#wrapped-literal"
    },
    {
      "include": "#cstring"
    },
    {
      "include": "#tag"
    },
    {
      "include": "#assignment-head"
    },
    {
      "include": "#assign"
    },
    {
      "include": "#keyword-structure"
    },
    {
      "include": "#keyword-builtin"
    },
    {
      "include": "#keyword-modifier"
    },
    {
      "include": "#number"
    },
    {
      "include": "#typeref"
    },
    {
      "include": "#fieldname-first"
    },
    {
      "include": "#alternative"
    },
    {
      "include": "#fieldname"
    }
  ],
  "repository": {
    "comment-line": {
      "comment": "X.680 12.6.3: a line comment ends at a newline or at a second --",
      "match": "--.*?(--|$)",
      "name": "comment.line.double-dash.asn1"
    },
    "comment-block": {
      "begin": "/\\*",
      "end": "\\*/",
      "name": "comment.block.asn1"
    },
    "hexstring": {
      "comment": "X.680 12.12: an hstring denotes a value, not text. Quotes around it make it look like a string, and calling it one once put 30 per cent of a real profile in the colour of prose, right beside the member names. As a numeric constant it is distinct in every default theme, which is also what it is.",
      "match": "'[0-9A-Fa-f\\s]*'[Hh]",
      "name": "constant.numeric.hex.asn1"
    },
    "binstring": {
      "comment": "X.680 12.11: a bstring, and a value for the same reason.",
      "match": "'[01\\s]*'[Bb]",
      "name": "constant.numeric.binary.asn1"
    },
    "wrapped-literal": {
      "comment": "An hstring or a bstring wrapped over lines, which X.680 12.11 and 12.12 allow because the whitespace inside one is not part of the value -- and which the writer does to every literal longer than a line. A TextMate match rule sees one line at a time, so the single-line rules above cannot reach these: the body of every wrapped literal in every published profile rendered colourless and its closing H as a type. The begin fires only when the rest of the line is hex digits and whitespace, which is what an unclosed literal looks like and a closed one never does. One name for both kinds, because every theme colours the two constant.numeric scopes alike and the split cannot be told from the opening line.",
      "begin": "'(?=[0-9A-Fa-f\\s]*$)",
      "end": "'[HhBb]",
      "name": "constant.numeric.hex.asn1"
    },
    "cstring": {
      "begin": "\"",
      "end": "\"",
      "name": "string.quoted.double.asn1"
    },
    "tag": {
      "comment": "[3], [APPLICATION 2], [PRIVATE 6]",
      "match": "\\[\\s*(UNIVERSAL|APPLICATION|PRIVATE|CONTEXT)?\\s*\\d*\\s*\\]",
      "name": "entity.other.attribute-name.tag.asn1"
    },
    "assignment-head": {
      "comment": "X.680 16.1: valuereference Type ::= Value. The reference is lower case and the type upper case, which is grammar and not convention. It is left uncoloured on purpose: it is module syntax around the value and not part of it, and euicc steps over it when reading. Listed before #alternative and #fieldname, which would otherwise claim the reference, since all three match at the same offset and the earlier rule wins.",
      "match": "(?<![-\\w])([a-z][A-Za-z0-9-]*)(\\s+)([A-Z][A-Za-z0-9-]*)(\\s*)(::=)",
      "captures": {
        "3": {
          "name": "entity.name.class.asn1"
        },
        "5": {
          "name": "keyword.operator.assignment.asn1"
        }
      }
    },
    "assign": {
      "match": "::=",
      "name": "keyword.operator.assignment.asn1"
    },
    "keyword-structure": {
      "match": "\\b(SEQUENCE|SET|CHOICE|ENUMERATED|OF|DEFINITIONS|BEGIN|END|IMPORTS|EXPORTS|FROM)\\b",
      "name": "keyword.control.asn1"
    },
    "keyword-builtin": {
      "match": "\\b(INTEGER|BOOLEAN|NULL|REAL|BIT|OCTET|STRING|OBJECT|IDENTIFIER|UTF8String|PrintableString|IA5String|NumericString|VisibleString|TeletexString|UniversalString|BMPString|GeneralizedTime|UTCTime|ANY|EXTERNAL|RELATIVE-OID)\\b",
      "name": "support.type.asn1"
    },
    "keyword-modifier": {
      "match": "\\b(OPTIONAL|DEFAULT|IMPLICIT|EXPLICIT|AUTOMATIC|TAGS|SIZE|WITH|COMPONENTS|COMPONENT|PRESENT|ABSENT|CONSTRAINED|BY|INCLUDES|MIN|MAX|TRUE|FALSE|UNION|INTERSECTION|EXCEPT)\\b",
      "name": "storage.type.asn1"
    },
    "number": {
      "match": "\\b\\d+\\b",
      "name": "constant.numeric.asn1"
    },
    "typeref": {
      "comment": "X.680 12.2: a type reference begins with an upper case letter. The lookbehind matters: a hyphen is a word boundary, so a plain \\b let this match Mandatory-services inside the field name eUICC-Mandatory-services, and the editor coloured half an identifier as a type.",
      "match": "(?<![-\\w])[A-Z][A-Za-z0-9-]*",
      "name": "entity.name.class.asn1"
    },
    "fieldname-first": {
      "comment": "The first word on a line, which in a value is a member name. #fieldname needs a value after the name to know it is one, so a name stayed the colour of nothing until the next word was typed, and half of writing a profile happened in white. A line begins a member often enough to settle it on its own. The lookahead hands a CHOICE alternative back to #alternative, which cannot win otherwise: this rule starts at the indent, further left, and the leftmost match is the one TextMate takes.",
      "match": "^\\s*([a-z][A-Za-z0-9-]*)(?![-\\w])(?!\\s*:[^:=])",
      "captures": {
        "1": {
          "name": "variable.other.property.asn1"
        }
      }
    },
    "alternative": {
      "comment": "X.680 29.2: an alternative of a CHOICE is written `name : value`. It selects one of a fixed set rather than naming a slot every value has, which is what an enum member is, so it takes that scope and the brighter blue #4FC1FF that themes give one. The colon is what tells the two apart, and it is the same distinction euicc has to make when reading.",
      "match": "(?<![-\\w])[a-z][A-Za-z0-9-]*(?=\\s*:[^:=])",
      "name": "variable.other.enummember.asn1"
    },
    "fieldname": {
      "comment": "A member name inside a value, and its scope names the KIND and nothing else: variable.other.property is what HCL's hcl-attrName pins and what TypeScript's semantic `property` resolves to, so a member here renders exactly as an attribute renders in Terraform and a key renders in TypeScript, theme by theme -- light blue in Dark Modern, the quiet foreground grey in the 2026 themes. Two earlier choices here engineered a colour instead: meta.object-literal.key collided with the 2026 string nineteen apart, and entity.name.tag made members green by claiming they were tags, which a reader rightly read as the wrong kind. A name that is itself the value, an ENUMERATED identifier, is followed by a comma or a closing brace, so the lookahead leaves it plain -- and the semantic layer marks it as a value from the schema.",
      "match": "(?<![-\\w])[a-z][A-Za-z0-9-]*(?=\\s+[^,}\\s])",
      "name": "variable.other.property.asn1"
    }
  }
}
